- DOM

- getElementsByClassName returns an array-like list, not a single element.

- query selector
let num = document.querySelector("#number");  // using ID
let box = document.querySelector(".box");     // using class
let p = document.querySelector("div p");      // nested element